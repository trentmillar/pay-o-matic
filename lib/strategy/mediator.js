var Types = require('../types');

var Mediator = function () {

    this.processors = {};


    var createCustomer = function (customer, callback) {
        if (!customer || arg.constructor !== Types.customer) {
            this.mediator.processors[arg.brand] = arg;
            return arg;
        }
        return this.mediator.processors[arg];
    };

};

Mediator.prototype.processor = require('../core/processor');

var mediator = module.exports = exports = new Mediator();

